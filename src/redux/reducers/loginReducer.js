const initialState = {
  isLogin: false,
  userInfo: "",
  token: "",
  error: ""
};

export const loginReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
